apiVersion: apps/v1
kind: Deployment
metadata:
  name: problem-service
  namespace: judgemesh
  labels: { app: problem-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: problem-service }
  template:
    metadata:
      labels: { app: problem-service }
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: /actuator/prometheus
        prometheus.io/port: "8082"
    spec:
      containers:
        - name: app
          image: docker.io/judgemesh/judgemesh-problem-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - { containerPort: 8082, name: http }
          env:
            - { name: SPRING_PROFILES_ACTIVE, value: prod }
            - { name: NACOS_ADDR, value: nacos.judgemesh-infra.svc:8848 }
            - { name: MYSQL_HOST, value: mysql.judgemesh-infra.svc }
            - { name: REDIS_HOST, value: redis.judgemesh-infra.svc }
            - { name: MINIO_ENDPOINT, value: http://minio.judgemesh-infra.svc:9000 }
          envFrom:
            - secretRef: { name: judgemesh-secrets }
          resources:
            requests: { cpu: 200m, memory: 512Mi }
            limits: { cpu: 1000m, memory: 1Gi }
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8082 }
            initialDelaySeconds: 30
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8082 }
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: problem-service
  namespace: judgemesh
spec:
  selector: { app: problem-service }
  ports:
    - { port: 8082, targetPort: 8082, name: http }
