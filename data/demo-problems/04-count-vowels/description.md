# Count Vowels

Given a lowercase string `s`, count how many characters in `s` are vowels.

Vowels are `a`, `e`, `i`, `o`, and `u`.

## Input

One line containing a lowercase string.

## Output

Print the number of vowels in the string.
